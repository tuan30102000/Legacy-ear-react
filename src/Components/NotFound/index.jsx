import React from 'react';
import './style.scss';
NotFound.propTypes = {
    
};

function NotFound(props) {
    return (
        <div>
            Không tt
        </div>
    );
}

export default NotFound;